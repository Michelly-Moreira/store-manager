const validName = 'ProdutoX';
const invalidValue = 'a';
const withoutName = ' ';

const allProducts = [
  {
    id: 1,
    name: validName,
  },
];

module.exports = {
  validName,
  invalidValue,
  withoutName,
  allProducts,
};